creat your python environment 

open terminal from the QT tool script path. Then type below command

$ pip install -r requirements.txt

it will install the packages. Now run the FMS.py.. there!!!! you go.. QT tool will open !!
